<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.eveb.saasops.modules.member.mapper.AuditMapper">

    <select id="finAuditList" resultType="com.eveb.saasops.modules.member.entity.MbrAuditAccount"
            parameterType="com.eveb.saasops.modules.member.entity.MbrAuditAccount">
        SELECT * FROM mbr_audit_account
        <where>
            <if test="accountId != null">
                AND accountId = #{accountId}
            </if>
            <if test="isDrawings != null">
                AND isDrawings = #{isDrawings}
            </if>
        </where>
        ORDER BY id
        <choose>
            <when test="sort == true">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
    </select>

    <select id="finAuditBonusList" resultType="com.eveb.saasops.modules.member.entity.MbrAuditBonus"
            parameterType="com.eveb.saasops.modules.member.entity.MbrAuditBonus">
        SELECT * FROM mbr_audit_bonus
        <where>
            <if test="accountId != null">
                AND accountId = #{accountId}
            </if>
            <if test="isDrawings != null">
                AND isDrawings = #{isDrawings}
            </if>
            <if test="depotId != null">
                AND depotId = #{depotId}
            </if>
            <if test="auditTime != null and auditTime != ''">
                AND time <![CDATA[ <= ]]> #{auditTime}
            </if>
        </where>
        ORDER BY id
        <choose>
            <when test="sort == true">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
    </select>

    <select id="findAuditAccountList" resultType="com.eveb.saasops.modules.member.dto.AuditDetailDto">
      SELECT * FROM (
            SELECT id,'0' auditType,time,depositAmount,auditAmount,validBet,remainValidBet,payOut,status,isOut,
            0 discountAmount,'' depotName,'' catName,'' activityName,'' isValid,'' isDispose,discardAmount
            FROM mbr_audit_account
            WHERE accountId=#{accountId}
            <if test="isDrawings != null">
                AND isDrawings = #{isDrawings}
            </if>
            <if test="startTime != null and startTime != ''">
                AND time <![CDATA[ >= ]]>#{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND time <![CDATA[ <= ]]>#{endTime}
            </if>
            UNION ALL
            SELECT bonus.id,'1'auditType,bonus.time,bonus.depositAmount,bonus.auditAmount,bonus.validBet,
            bonus.remainValidBet,bonus.payOut,bonus.status,bonus.isOut,bonus.discountAmount,bonus.depotName,
            cat.catName,activity.activityName,bonus.isValid,bonus.isDispose,null discardAmount
            FROM mbr_audit_bonus bonus LEFT JOIN t_gm_cat cat on bonus.catId = cat.id
            LEFT JOIN opr_act_activity activity ON bonus.activityId = activity.id
            WHERE bonus.accountId=#{accountId}
            <if test="isDrawings != null">
                AND bonus.isDrawings = #{isDrawings}
            </if>
            <if test="startTime != null and startTime != ''">
                AND bonus.time <![CDATA[ >= ]]>#{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND bonus.time <![CDATA[ <= ]]>#{endTime}
            </if>
      ) a ORDER BY a.time desc
    </select>

    <select id="findAuditAccountId" resultType="java.lang.Integer">
      SELECT DISTINCT a.accountId FROM
      (SELECT accountId FROM mbr_audit_account
      UNION ALL SELECT accountId FROM mbr_audit_bonus) a
      <where>
          <if test="accountId != null">
              AND a.accountId =#{accountId}
          </if>
      </where>
    </select>

    <select id="findMbrAuditHistory" resultType="com.eveb.saasops.modules.member.entity.MbrAuditHistory"
            parameterType="com.eveb.saasops.modules.member.entity.MbrAuditHistory">
        SELECT * FROM mbr_audit_history
        <where>
            <if test="isSign != null">
                AND isSign =#{isSign}
            </if>
            <if test="accountId != null and accountId != ''">
                AND accountId =#{accountId}
            </if>
        </where>
        ORDER BY startTime DESC
    </select>

    <select id="fundAuditBonusByTime" resultType="com.eveb.saasops.modules.member.entity.MbrAuditBonus"
            parameterType="com.eveb.saasops.modules.member.entity.MbrAuditBonus">
        SELECT * FROM mbr_audit_bonus WHERE accountId =#{accountId}
        <if test="isDrawings != null">
            AND isDrawings =#{isDrawings}
        </if>
        <if test="startTime != null and startTime != ''">
            AND time <![CDATA[ >= ]]>#{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND time <![CDATA[ <= ]]>#{endTime}
        </if>
        ORDER BY time
    </select>
</mapper>